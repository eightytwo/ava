- if @category.errors.any?
  #error_explanation
    %h3= t("form.error_title", count: @category.errors.count)

    %ul
    - @category.errors.full_messages.each do |msg|
      %li= msg

.form_content
  = form_for(@category) do |f|
    - if f.object.new_record?
      = f.hidden_field :organisation_id

    .field_wrapper
      = f.label :organisation
      = @category.organisation.name
    
    .field_wrapper
      = f.label :name
      = f.text_field :name, autofocus: true
    
    .field_wrapper
      = f.submit({class: "button small green"})

    .links_wrapper
      = link_to t("links.back"), audio_visual_categories_path(oid: @organisation.id)
